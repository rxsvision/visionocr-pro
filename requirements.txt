# VisionOCR Pro - Core Dependencies
gradio>=5.0
openpyxl>=3.1
matplotlib>=3.7
PyMuPDF>=1.23
Pillow>=10.0
requests>=2.28
pyyaml>=6.0
apscheduler>=3.10

# OCR Engines (optional, install as needed)
# rapidocr-onnxruntime>=1.3
# paddleocr>=2.7
# paddlepaddle-gpu  # see https://www.paddlepaddle.org.cn

# LLM (optional)
# transformers>=4.40
# accelerate>=0.30

# 3D 深度相机 Sizector (Phase 4C, optional)
# 仅 Windows + .NET Framework 4.7.2+ 需要; 通过 .NET wrapper 调用 MPSizectorS SDK
# pythonnet>=3.0
