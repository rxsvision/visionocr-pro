# VisionOCR Pro - Python 依赖
# Python 3.10+, CUDA 12.x

# === 核心框架 ===
gradio>=4.44.0
pyyaml>=6.0
pillow>=10.0
numpy>=1.24
opencv-python>=4.9

# === 数据库 ===
# SQLite 内置, 无需额外依赖

# === OCR 引擎 ===
# OvisOCR2 (印刷文档 SOTA)
# 安装: pip install ovis-ocr  (或从源码)
# ovis-ocr>=1.0

# PaddleOCR-VL-1.6 (相机照片 SOTA)
# 安装: pip install paddleocr-vl
# paddleocr-vl>=1.6

# RapidOCR / PP-OCRv6 (CPU 兜底)
rapidocr-onnxruntime>=1.4

# HunyuanOCR (手写体 SOTA) - 需手动安装
# 参考: https://github.com/Tencent/HunyuanOCR

# Unlimited-OCR (长文档)
# unlimited-ocr>=1.0

# MinerU (结构化提取)
# magic-pdf>=2.5

# === 视觉检测 ===
# RF-DETR Large
# rfdetr>=1.0

# Grounding DINO
# groundingdino (从源码安装)

# SAM 3
# segment-anything-3

# Anomalib (工业缺陷)
# anomalib>=2.0

# === 条码 ===
pyzbar>=0.1.9
# opencv-contrib-python 含 WeChat QR (已含在 opencv-python 中)

# === VLM / LLM ===
ollama>=0.4
openai>=1.30               # OpenAI 兼容 API 客户端

# === 姿态 / 行为 (P2) ===
# mmpose (RTMPose) - 需 mmcv + mmdet
# mmpose>=1.3

# === 工具 ===
httpx>=0.27
tqdm>=4.66
platformdirs>=4.0

# === 可选: 工业相机 ===
# harvesters>=1.4          # GigE Vision 跨平台
# aravis (Linux only)
