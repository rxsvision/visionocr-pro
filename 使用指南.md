# VisionOCR Pro v1.5.0 使用指南

> 通用视觉识别与工业检测平台 — OCR 识别 / 合同自动化 / 工业质检 一站式本地应用
> 详细部署与故障排查见 [DEPLOY.md](DEPLOY.md)，新产品接入见 [docs/new_product_onboarding.md](docs/new_product_onboarding.md)。

---

## 1. 安装与启动

### 1.1 环境要求

| 必装 | 版本 |
|------|------|
| Python | 3.11 – 3.13（不支持 3.14） |
| NVIDIA 驱动 | ≥ 525（CUDA 12.x） |
| Git | 2.x+ |
| Ollama | 最新版（合同 LLM 抽取需要） |

### 1.2 一键部署

```bash
git clone https://github.com/rxsvision/visionocr-pro.git
cd visionocr-pro
setup.bat        # Windows（自动: venv → PyTorch → 依赖 → 模型 → doctor 自检）
# ./setup.sh     # Linux / Jetson
```

首次运行约 15–40 分钟（下载模型），全程无需管理员权限。

### 1.3 启动与自检

```bash
run.bat                                        # Windows 启动，浏览器打开 http://localhost:7860
.venv\Scripts\python.exe scripts\doctor.py     # 环境体检（核心依赖失败报 FAIL）
```

注意：不要用系统裸 `python` 直接跑仓库脚本，统一经 `run.bat` 或 `.venv\Scripts\python.exe`。

---

## 2. 工人 / 工程师双模式

界面右上角 Toggle 切换：

- **工人模式**：隐藏所有调参，一键拍照获得 OK/NG 判定，适合产线操作。
- **工程师模式**：完整控制（检测模式选择、建库、校准、阈值、引擎健康面板）。

---

## 3. OCR 识别 Tab

1. 上传或拍摄图像 → 自动执行：质量预检 → 场景分类器自动选引擎 → 透视纠偏 → 图像增强 → OCR 双路径对比取优 → 正则纠错。
2. 置信度 ≥ 阈值（默认 0.75，`ocr.confidence_threshold`）判 OK；低于阈值标记「待人工复核」。
3. 每次推理写入审计日志（SQLite），条码自动并行检测写入结果 meta。
4. 引擎选择：RapidOCR（默认轻量）/ PP-OCRv6（高精度，需 Docker）/ OvisOCR2 / PaddleOCR-VL 等，多引擎 LRU 显存管理自动切换。

---

## 4. 合同自动化 Tab

### 4.1 上传与抽取

1. 上传合同（PDF/JPG/PNG；单文件上限 `contracts.max_upload_mb`，默认 50 MB）。
2. 管线：SHA256 去重 → 文档读取 → 分级 LLM 抽取（本地 Ollama → 云端 API → 规则兜底）→ 方向判定（应收/应付）→ 金额勾稽校验 → 风险扫描 → 落库。

### 4.2 人工复核与批量审批

- 每份合同进入「待复核」队列，可逐份确认或驳回。
- **批量通过**：设置置信度阈值一键通过；v1.5.0 起高置信合同还必须通过结构化校验（金额勾稽）才会自动通过，否则拦截转人工复核。

### 4.3 回款提醒

- 四级提醒：逾期 / 7 天 / 3 天 / 1 天，由 APScheduler 自动推送桌面通知。
- 手动触发：`python scripts/run_reminder.py`。

---

## 5. 工业质检 Tab（核心）

### 5.1 四种检测模式（工程师模式四选一）

| 模式 | 适用场景 | 说明 |
|------|----------|------|
| ① 零样本 Grounding DINO | 新产品试产、无样本 | 提示词驱动，中文缺陷词自动翻译 |
| ② 少样本 PatchCore | 有 ≥50 张 OK 图 | 建库后可叠加 3D 深度融合 |
| ③ Union 零漏检 | 生产主路径 | PatchCore + DINO + YOLO + DINOv2 四源 OR，任一 NG 即 NG |
| ④ 快速换线辅助 SubspaceAD | 换线过渡期 | 1–4 张 OK 图极速建库，仅供参考，须人工复核 |

检测流程 v1.5.0 起单次解码直通各引擎（热路径提速），NG 后可点「AI 解释」触发智能 ROI 裁切 + 本地 VLM 局部识读。

### 5.2 新产品接入五步法（详见 docs/new_product_onboarding.md）

1. 采集 ≥50 张 OK 图（光照/角度 3 组以上）。
2. PatchCore + DINOv2 双建库（工程师面板建库区）。
3. NP 校准：`qc.patchcore.np_epsilon` / `qc.dinov2.np_epsilon`（默认 0.10）。
4. 建库后补采 ≥30 张独立 OK 图跑「📐 校准协议」重标定阈值。
5. NG 回归验证 Recall=100% → 双条件放行。

### 5.3 YOLO 产品门控

- 权重按产品绑定：`models/yolo/{产品名}.pt`。
- 无该产品权重时 Union 自动跳过 YOLO 源（防跨域误报）。
- 训练：`python finetune/train_yolo.py`（默认 yolo11n）。

### 5.4 质检看板（可选）

```bash
pip install datasette
python scripts/qc_dashboard.py --port 8901    # 日统计 + NG 明细 + 缺陷图回溯
```

检测图自动持久化到 `data/qc_images/`（内容哈希去重），看板图片链接不失效。

---

## 6. 配置要点（config.yaml）

| 配置项 | 说明 |
|--------|------|
| `company.name` / `company.aliases` | 我方主体名称（合同方向判定依据） |
| `llm.ollama.model` | 本地 LLM 模型名（默认 qwen3-vl:8b） |
| `llm.api.api_key` | 云端 API 密钥（可选兜底，建议放 .env） |
| `ocr.confidence_threshold` | OCR 置信度拦截阈值（默认 0.75） |
| `contracts.max_upload_mb` | 合同上传大小上限（默认 50） |
| `qc.patchcore.np_epsilon` / `qc.dinov2.np_epsilon` | NP 校准目标误报率（默认 0.10） |
| `qc.vlm_explain` | NG AI 解释开关与 ROI 参数 |
| `camera.type` | 相机类型（hikvision / opencv / gigevision） |

多产线配置：`profiles/` 目录 + `--profile` 启动参数切换。

---

## 7. 数据与运维

| 项目 | 位置 / 命令 |
|------|-------------|
| 数据库 | `data/visionocr.db`（SQLite WAL，启动自动备份到 `data/backups/`，5 份轮转） |
| 特征库 | `data/banks/`（PatchCore）、`data/banks_dinov2/`、`data/banks_subspacead/` |
| 日志 | `logs/visionocr.log` + 结构化 `logs/visionocr.jsonl` |
| 环境体检 | `scripts/doctor.py` |
| 单元测试 | `.venv\Scripts\python.exe -m pytest`（294 tests） |

安全说明（v1.5.0）：

- 特征库 NPZ 加载默认拒绝 pickle 对象数组（`core/npz_io.py`），仅自产可信库显式回退。
- 产品名路径清洗防穿越；特殊字符文件名清洗为 `_`（旧文件需手动重命名匹配）。

---

## 8. 常见问题

| 问题 | 处理 |
|------|------|
| 启动后引擎加载慢 | 首次加载需下载/载入权重；后续由预热机制后台预加载 |
| 显存不足 | LRU 自动驱逐非常驻引擎；`vram.idle_unload_sec` 空闲卸载（默认 1800s） |
| OCR 结果不准 | 调低场景分类器旁路阈值或切换引擎；检查图像质量预检是否提示模糊 |
| 质检误报多 | 调小 `np_epsilon`（更保守）；或补采 OK 图重跑校准协议 |
| 质检漏检担忧 | 使用 Union 零漏检模式（四源 OR），误报由人工复核兜底 |
| Windows 上 paddle GPU 报错 | 不要装 paddlepaddle-gpu（cudnn 冲突），用 CPU 版或 Docker 方案（见 DEPLOY.md §6） |
| 合同方向判错 | 检查 `company.name` 与实际合同主体名称是否一致 |
| 看板图片 404 | 确认检测图已持久化到 `data/qc_images/`；重启看板 |

---

*VisionOCR Pro — 苏州锐新视科技有限公司 | 许可: BSL 1.1*
